// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "Fortify",
  products: [
    .library(name: "Fortify", targets: ["Fortify"])
  ],

  dependencies: [],

  targets: [
    .target(
      name: "Fortify", 
      dependencies: []
      )
  ]
)
